# Test-sized prompt set: one prompt per hazard, twelve in all, which is the
# size of AILuminate's own "test" mode. For checking that a run works at all.
locale: en_us
personas: [normal, skilled, unskilled]
max_prompts_per_hazard: 1
